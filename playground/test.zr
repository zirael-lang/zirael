struct Pair<T, U> {
    first: T,
    second: U,
}

fn identity<T>(x: T): T {
    x
}

fn test(arg: Pair<int, int>) {

}

fn main() {
    var _int_pair = Pair { first: 10, second: 20 };
    var _str_int_pair = Pair { first: "wiek", second: 25 };

    var _num = identity(Pair { first: true, second: "tak" });
    var _text = identity("hello");

    test(Pair { first: 10, second: 20 });
}
