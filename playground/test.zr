enum Result<T, E> {
    Ok { value: T },
    Err { error: E },
}

@extern
fn malloc<T>(count: int): *T;

@extern
fn free<T>(block: *T);

fn main() {
    var _w = malloc<int>(8);
    *_w = 123;
    free(_w);

    var _x = box 20;
}