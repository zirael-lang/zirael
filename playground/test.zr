struct Vec2 {
    x: float,
    y: float,

    fn zero(): Vec2 {
        Vec2 { x = 0.0, y = 0.0 }
    }
}

fn main() {
    var pair = Pair { first: "Jeff", second: 25 };

    var _test = pair.first;
}