enum Result<T, E> {
    Ok { value: T },
    Err { error: E },
}

fn divide(a: int, b: int): Result<int, bool> {
    b == 0 ? Result::Err(false) : Result::Ok(a / b)
}

fn main() {
    var _result = divide(10, 2);

    var _res = match divide(10, 0) {
        Result::Ok { value } => {
            true
        },
        Result::Err { error } => {
            error
        }
    };
}